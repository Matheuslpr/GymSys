version: '3.8'
services:
  db:
    image: postgres:16-alpine
    container_name: gymsys
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: gym
    ports:
      - "5431:5434"
    restart: always
